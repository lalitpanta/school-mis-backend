For database conncetivity.
->Commands
npm install pg db-migrate db-migrate-pg dotenv

In env I have updated with
    DB_HOST=localhost
    DB_PORT=5432
    DB_NAME=your_database_name
    DB_USER=your_pg_username
    DB_PASSWORD=your_pg_password
    DB_POOL_MIN=2
    DB_POOL_MAX=10
    